

/*
Adds a new task to the system.
Removes a task from the system by ID.
Finds and returns a pointer to the task with the given ID. Returns nullptr if not found.
Returns a list of tasks with the given status.
Returns a list of all tasks in the system.
Updates the status of a task by ID.


noun and verbs:
ID (integer, unique identifier)
Title (string)
Status (enum: ToDo, InProgress, Done)
Priority (enum: Low, Medium, High)

task: 
system: list <tasks*>


*/
